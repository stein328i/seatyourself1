<h1>index page</h1>


<div class='label'>
<% @restaurants.each do |restaurant| %>
<p><%= restaurant.name %><%= link_to(image_tag(restaurant.url, size: '300'), restaurant_path(restaurant), class: "restaurant") %></p>

<% end %>
</div>
