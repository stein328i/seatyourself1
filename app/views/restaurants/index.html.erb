<h1>index page</h1>
<% @restaurants.each do |restaurant| %>
<p><%= link_to(image_tag(restaurant.url), restaurant_path(restaurant), class: "restaurant") %>
<%=image_tag restaurant.menu %></p>
<% end %>
